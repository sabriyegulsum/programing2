#include <stdio.h>
int main()
{
    // enumda en önemli şey değerleri içindeki sabit olmalı kodun içerisinde alamaszin veya değiştiremezsin
    enum sanfoundry
    {
        a=1,b,c,d,e
    };
    printf("%d",b*c+e-d);
    return 0;
}